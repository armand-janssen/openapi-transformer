components:
  schemas:
    componentA:
      type: object
      description: Component A.
      properties:
        id:
          $ref: 'parentSchema.yaml#/components/schemas/id'
        name:
          type: string
          description: Component A's name.